flask==2.3.3
flask-cors==4.0.0
python-docx==1.0.0
PyPDF2==3.0.1
openai==1.14.0
python-dotenv==1.0.0
werkzeug==2.3.7
httpx==0.27.0
# The core dependencies above are still required for compatibility
# No additional packages needed for DeepInfra as we're using httpx which is already included